[package]
name = "linux_dev"
version = "0.1.0"
authors = [ "various" ]

[lib]
name = "linux_dev"
path = "lib.rs"

[features]
default = ["retry"]
retry = []

[dependencies.linux_core]
path = "../core"
