[package]
name = "linux_clock"
version = "0.1.0"
authors = [ "various" ]

[lib]
name = "linux_clock"
path = "lib.rs"

[features]
default = ["retry"]
retry = []

[dependencies.linux_core]
path = "../core"
