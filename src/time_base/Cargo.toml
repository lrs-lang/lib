[package]
name = "linux_time_base"
version = "0.1.0"
authors = [ "various" ]

[lib]
name = "linux_time_base"
path = "lib.rs"

[features]
default = ["retry"]
retry = []

[dependencies.linux_core]
path = "../core"
