[package]
name = "linux_poll"
version = "0.1.0"
authors = [ "various" ]

[lib]
name = "linux_poll"
path = "lib.rs"

[features]
default = ["retry"]
retry = []

[dependencies.linux_core]
path = "../core"
